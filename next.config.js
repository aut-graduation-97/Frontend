/** @type {import('next').NextConfig} */

const nextConfig = {
  // TODO: remove later
  images: {
    domains: ['i.pravatar.cc'],
  },
  reactStrictMode: true,
  swcMinify: true,
};

module.exports = nextConfig;
